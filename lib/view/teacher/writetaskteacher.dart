  import 'package:flutter/material.dart';

  class WriteTask extends StatelessWidget {
    const WriteTask({Key? key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          backgroundColor: const Color(0xff35007D),
        ),

      );
    }
  }
